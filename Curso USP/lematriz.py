def cria_matriz(tot_lin, tot_col, valor):
    matriz = []  #lista vazia
    for i in range(tot_lin):
        linha = []
        for j in range(tot_col):
            print(mat[i] "x" mat[j])
            linha.append(valor)
        matriz.append(linha)
    return matriz
mat= [[1], [2], [3]]
